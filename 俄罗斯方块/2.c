#include <stdio.h>  // 必须包含的头文件
#include <windows.h> // 若用到SetConsoleCP等中文编码函数，需加

int main() {
    // 解决字符串换行问题：每行末尾加\，或用""拼接
    SetConsoleCP(65001); // 设置控制台编码为UTF-8，避免中文乱码（可选）
    printf("俄罗斯方块（Tetris）是一款经典的益智电子游戏，\
由俄罗斯工程师阿列克谢·帕基特诺夫（Alexey Pajitnov）于1984年6月发明，\
最初运行在Electronika 60计算机上。1989年，任天堂将其推广到全球，\
使其成为有史以来最受欢迎的电子游戏之一。\n");

    printf("游戏名称中的\"Tetris\"来源于希腊语前缀\"tetra-\"（意为\"四\"），\
结合了开发者帕基特诺夫喜爱的网球（tennis）运动。这一命名也贴合游戏核心：\
所有方块均由4个小方格组成。俄罗斯方块的规则简单却极具策略性，\
玩家需通过旋转、移动下落的方块，将其排列成完整的一行或多行以消除得分。\n");

    printf("这款游戏不仅是娱乐经典，更因其易学难精的特性, \
成为了全球流行的文化符号，甚至被用于心理学研究，\
探索人类的空间认知、决策能力和注意力分配。\n");
    return 0;
}